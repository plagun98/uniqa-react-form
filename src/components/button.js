import React, {useState} from "react";

const Button = () => {

    return(
        <button type="submit" className="btn btn-primary col-md-2 col-sm-4 col-sx-6 mx-auto">Submit</button>
    );
}

export default Button;